var diam =20;
var strokeColor=(0, 0, 0);
var isGrowing=true;
var centX, centY;

function setup(){
  createCanvas(windowWidth, windowHeight);
  frameRate(24);
  smooth();
  background
  centX=width/2;
  centY
}